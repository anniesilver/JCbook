/**
 * Credentials Tab Route
 * Renders the CredentialStorageScreen for the credentials tab
 */

import React from 'react';
import { CredentialStorageScreen } from '@/screens/credential/CredentialStorageScreen';

/**
 * Credentials screen component for the tabs navigation
 */
export default function CredentialsRoute() {
  return <CredentialStorageScreen />;
}
